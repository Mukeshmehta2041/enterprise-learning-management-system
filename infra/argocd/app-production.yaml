apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lms-backend-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/learning-managment-system.git
    targetRevision: main
    path: infra/helm/lms-backend
    helm:
      valueFiles:
        - values-production.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: lms-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
