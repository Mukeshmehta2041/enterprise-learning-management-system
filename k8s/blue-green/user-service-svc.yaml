apiVersion: v1
kind: Service
metadata:
  name: lms-user-service
  namespace: lms
spec:
  ports:
  - port: 8081
    targetPort: 8081
  selector:
    app: lms-user-service
    version: blue # Change this to 'green' for traffic shift
